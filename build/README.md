# Build Artifacts

This directory contains historical workflow scripts and shell helpers from the initial
reverse-engineering and hardening sprint. They are retained as process evidence and are not part of
the npm package surface.

Some scripts include capture-time absolute paths from the machine where the sprint ran. Those paths
are historical metadata only; current reproducible verification commands are documented in
`PROGRESS.md`.
